{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug QEMU",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/build/os.bin",
            "miDebuggerPath": "/usr/bin/gdb",
            "miDebuggerServerAddress": "localhost:1234",
            "preLaunchTask": "Build and run with debugging",
            "stopAtEntry": true,
            "cwd": "${workspaceFolder}",
            "MIMode": "gdb"
        }
    ]
}